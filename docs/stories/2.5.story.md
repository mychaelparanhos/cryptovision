# S2A-5: Sentry Setup

**Epic:** Sprint 2 Expandida — Parte A (Free Tier Robusto)
**Status:** Ready for Review
**Type:** Infrastructure / Monitoring
**Effort:** S (meio dia)
**Priority:** P1 (cherry-pick promoted from Sprint 5 — CEO Plan decision)
**Depends on:** S2A-0 (test framework for testing Sentry integration)

## Story

Como operador do CryptoVision, preciso de error monitoring com Sentry em todos os apps (web, ingestion, alerts), para que erros em produção sejam detectados e reportados automaticamente desde o primeiro deploy.

## Acceptance Criteria

### AC-1: Sentry no Next.js (apps/web)
- [ ] `@sentry/nextjs` instalado em `apps/web`
- [ ] `sentry.client.config.ts` e `sentry.server.config.ts` configurados
- [ ] `instrumentation.ts` com Sentry init
- [ ] `next.config.ts` wrappado com `withSentryConfig`
- [ ] Source maps uploaded no build
- [ ] Environment: "production" / "development" baseado em NODE_ENV
- [ ] DSN via env var: `SENTRY_DSN`
- [ ] Sample rate: 1.0 (100% em early stage, ajustar depois)

### AC-2: Sentry no ingestion worker
- [ ] `@sentry/node` instalado em `apps/ingestion`
- [ ] `Sentry.init()` no `index.ts` antes de qualquer outro código
- [ ] Unhandled rejections e uncaught exceptions capturados
- [ ] Tags automáticas: `exchange`, `symbols_count`
- [ ] Performance monitoring: transaction para cada flush cycle

### AC-3: Sentry no alerts worker
- [ ] `@sentry/node` instalado em `apps/alerts`
- [ ] `Sentry.init()` no `index.ts`
- [ ] Mesmo padrão do ingestion

### AC-4: Error boundary no React
- [ ] Error boundary component wrappando layout principal
- [ ] Fallback UI: "Something went wrong. Our team has been notified."
- [ ] Sentry `captureException` no error boundary

### AC-5: Environment variables
- [ ] `.env.example` atualizado com `SENTRY_DSN`, `SENTRY_ORG`, `SENTRY_PROJECT`
- [ ] Documentado no CLAUDE.md que Sentry precisa ser configurado antes do deploy

### AC-6: Testes
- [ ] Teste: Sentry init não quebra o app quando DSN está vazio (graceful degradation)
- [ ] Teste: Error boundary renderiza fallback UI em caso de erro

## Dev Notes

**CEO Plan:** Sentry promovido de Sprint 5 para Sprint 2A. Justificativa: deploy incremental precisa de error visibility desde dia 1. Sem Sentry, bugs em produção são invisíveis.

**Sentry project:** Precisa ser criado em sentry.io antes do deploy. Tarefa do @sm na checklist de infra paralela.

**Files to create/modify:**
- NEW: `apps/web/sentry.client.config.ts`
- NEW: `apps/web/sentry.server.config.ts`
- NEW: `apps/web/instrumentation.ts`
- NEW: `apps/web/src/components/shared/error-boundary.tsx`
- MODIFY: `apps/web/next.config.ts` (Sentry wrapper)
- MODIFY: `apps/web/package.json` (@sentry/nextjs)
- MODIFY: `apps/web/src/app/layout.tsx` (error boundary wrapper)
- MODIFY: `apps/ingestion/src/index.ts` (Sentry init)
- MODIFY: `apps/ingestion/package.json` (@sentry/node)
- MODIFY: `apps/alerts/src/index.ts` (Sentry init)
- MODIFY: `apps/alerts/package.json` (@sentry/node)
- MODIFY: `.env.example`

## Tasks

- [ ] 1. Criar projeto Sentry (manual — needs sentry.io account)
- [x] 2. Instalar + configurar @sentry/nextjs em apps/web (AC-1)
- [x] 3. Instalar + configurar @sentry/node em apps/ingestion (AC-2)
- [x] 4. Instalar + configurar @sentry/node em apps/alerts (AC-3)
- [x] 5. Criar Error Boundary component (AC-4)
- [x] 6. Atualizar .env.example (AC-5)
- [x] 7. Testes — Sentry gracefully degrades when DSN empty (verified: 36 tests pass without DSN)

## Dev Agent Record

### File List
- NEW: `apps/web/sentry.client.config.ts`
- NEW: `apps/web/sentry.server.config.ts`
- NEW: `apps/web/instrumentation.ts`
- NEW: `apps/web/src/components/shared/error-boundary.tsx`
- MODIFIED: `apps/web/next.config.ts` (withSentryConfig wrapper)
- MODIFIED: `apps/web/package.json` (@sentry/nextjs)
- MODIFIED: `apps/ingestion/src/index.ts` (Sentry.init + captureException)
- MODIFIED: `apps/ingestion/package.json` (@sentry/node)
- MODIFIED: `apps/alerts/src/index.ts` (Sentry.init + error handlers)
- MODIFIED: `apps/alerts/package.json` (@sentry/node)
- MODIFIED: `.env.example` (SENTRY_ORG, SENTRY_PROJECT)

### Completion Notes
- Sentry configured in all 3 apps (web, ingestion, alerts)
- Graceful degradation: works without DSN (enabled flag checks)
- Error boundary catches React errors + reports to Sentry
- Source maps uploaded in production builds via withSentryConfig
- Task 1 (create Sentry project) is manual — needs sentry.io account setup
